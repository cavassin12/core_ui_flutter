/// Modo de seleção do `DatePickerDefault`: apenas data, apenas hora, ou data
/// e hora combinadas (equivalente aos tipos `date`/`time`/`datetime-local`
/// de um input HTML, e ao `type` do `InputDefault`).
enum DatePickerModoDefault { data, hora, dataHora }
